on: push
name: Make stub

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  collate:
    name: collate ${{ matrix.python }} ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
        - macos-latest
        - windows-latest
        - ubuntu-latest
        python:
        - '3.14'
        - '3.13'
        - '3.12'
        - '3.11'
        - '3.10'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      id: can-setup-python
      continue-on-error: true
      with:
        python-version: ${{ matrix.python }}

    - name: Collate errnos
      if: ${{ !cancelled() && steps.can-setup-python.outcome == 'success' }}
      run: |
        python collate.py | tee errnos.py

    - name: Upload output
      uses: actions/upload-artifact@v4
      with:
        name: errnos-${{ matrix.os }}-${{ matrix.python }}
        path: errnos.py

  combine:
    needs: collate
    runs-on: ubuntu-latest
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Display collected outputs
      run: |
        find . -name "errnos.py" -type f | while read -r file; do
          cat "$file" >> errnos-combined.py
        done

    - name: Upload combined errnos
      uses: actions/upload-artifact@v4
      with:
        name: output-combined
        path: errnos-combined.py

  dedupe:
    needs: combine
    name: dedupe (using ${{ matrix.os }} ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
        - ubuntu-latest
        python:
        - '3.14'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Generate deduped stub
      run: |
        python dedupe.py output-combined/errnos-combined.py
